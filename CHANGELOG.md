# Changelog

## [Unreleased]
### Added
- New CLI flags `--init`, `--x0`, `--sigma`, `--k0`, and `--U0` for configuring Gaussian packets and uniform fields. Default initial state is now the complex Gaussian packet.
